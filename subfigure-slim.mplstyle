# Make subfigure plots slightly narrower so that they fit side-by-side on the page
# Also, make them slightly shorter (i.e., 'slim')

figure.figsize : 2.9, 2.2

legend.fontsize : 9
axes.labelsize : 9
xtick.labelsize : 9
ytick.labelsize : 9
